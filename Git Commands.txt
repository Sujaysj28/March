To check the status of Repository 
git status

git clone " HTTPS Link of Repository"

git config --global user.email "sujayjain28@gmail.com"
git config --global user.name "Sujaysj28"

git add --all

git add "Filename"

git commit -m "Message"

git push